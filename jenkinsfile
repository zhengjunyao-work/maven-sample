stages {
        stage('maven install') {
          steps {
              withMaven {
                sh 'mvn clean install'
                        // some block
                 
              }
            // One or more steps need to be included within the steps block.
          }
        }

}
